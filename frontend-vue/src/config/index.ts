export const API_URL: string = import.meta.env.VITE_API_URL;

export const WEBSOCKETS: string = import.meta.env.VITE_WEBSOCKETS;

export const APP_URL: string = import.meta.env.VITE_APP_URL;

export const APP_PORT: number = parseInt(import.meta.env.VITE_APP_PORT);
